<div class="flex items-center space-x-1">
    <Button variant="ghost" class="group relative px-4" href="#features">
        <span class="relative z-10">Features</span>
        <span
            class="absolute inset-0 scale-0 rounded-md bg-primary/10 transition-transform duration-200 group-hover:scale-100"
        ></span>
    </Button>
    <Button variant="ghost" class="group relative px-4" href="#demo">
        <span class="relative z-10">Demo</span>
        <span
            class="absolute inset-0 scale-0 rounded-md bg-primary/10 transition-transform duration-200 group-hover:scale-100"
        ></span>
    </Button>
    <Button variant="ghost" class="group relative px-4" href="#pricing">
        <span class="relative z-10">Pricing</span>
        <span
            class="absolute inset-0 scale-0 rounded-md bg-primary/10 transition-transform duration-200 group-hover:scale-100"
        ></span>
    </Button>
    <Button variant="ghost" class="group relative px-4" href="#contact">
        <span class="relative z-10">Contact</span>
        <span
            class="absolute inset-0 scale-0 rounded-md bg-primary/10 transition-transform duration-200 group-hover:scale-100"
        ></span>
    </Button>
    <Button class="ml-2 bg-primary/90 transition-colors duration-200 hover:bg-primary"
        >Get Started</Button
    >
</div>

<script>
import {Button} from '$ui/button/index.js'
</script>
